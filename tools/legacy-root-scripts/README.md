# Legacy Root Scripts

This folder contains standalone scripts that used to live at the repo root but
are not part of the app runtime.

Examples:

- emulator/debug helpers
- one-off translation helpers
- local verification scripts
- Firestore inspection helpers

Keep them here unless they are either:

- deleted as obsolete, or
- promoted into a maintained workflow under `scripts/` or another structured
  tooling area
