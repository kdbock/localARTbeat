name: artbeat_events
description: Event management package for ARTbeat - handles event creation, ticketing, and management
version: 1.0.1
publish_to: none # This package is not intended for publishing

environment:
  sdk: ">=3.10.7 <4.0.0"
  flutter: ">=3.38.7"

dependencies:
  flutter:
    sdk: flutter

  # Firebase dependencies
  firebase_core: ^4.4.0
  firebase_auth: ^6.1.4
  cloud_firestore: ^6.1.2
  firebase_storage: ^13.0.6

  # Stripe integration for event ticketing
  flutter_stripe: ^12.2.0  # Latest with enhanced ProGuard rules

  # Notifications
  flutter_local_notifications: ^21.0.0
  awesome_notifications: ^0.11.0

  # Image handling
  image_picker: ^1.0.7
  cached_network_image: ^3.4.1
  flutter_cache_manager: ^3.4.1

  # Date/time handling
  intl: ^0.20.2
  timezone: ^0.11.0

  # Chart visualization for analytics
  fl_chart: ^1.2.0

  # HTTP requests
  http: ^1.2.0

  # State management
  provider: ^6.1.1

  # Local storage
  shared_preferences: ^2.5.3

  # Logging
  logger: ^2.4.0

  # Localization
  easy_localization: ^3.0.7
  package_info_plus: ^9.0.0

  # Fonts
  google_fonts: ^8.0.2

  # Local packages
  artbeat_core:
    path: ../artbeat_core
  artbeat_ads:
    path: ../artbeat_ads

  # QR Code generation
  qr_flutter: ^4.1.0

  # Other dependencies
  uuid: ^4.3.3
  share_plus: ^12.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  mockito: ^5.4.2
  build_runner: ^2.4.9
  fake_cloud_firestore: ^4.0.0

  cloud_firestore_platform_interface: ^7.1.0
flutter:
  uses-material-design: true
