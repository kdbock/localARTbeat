{
  "main": "src/index.js",
  "scripts": {
    "genkit:start": "genkit start -- tsx --watch src/genkit-sample.ts",
    "lint": "eslint --ext .js,.ts .",
    "test": "node --test test/adminRoleContract.test.js test/paymentAuthGuards.test.js test/paymentEndpointAuthMatrix.test.js test/storageRulesContentType.test.js test/dataRequestSla.test.js",
    "test:rules:setup": "npm --prefix test/rules install",
    "test:rules": "npm run test:rules:setup && firebase emulators:exec --project wordnerd-artbeat --only firestore \"NODE_PATH=./test/rules/node_modules node --test test/firestore.rules.test.js\"",
    "test:storage-rules": "npm run test:rules:setup && firebase emulators:exec --project wordnerd-artbeat --only storage,firestore \"NODE_PATH=./test/rules/node_modules node --test test/storageMimeMatrix.rules.test.js\"",
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions",
    "shell": "npm run build && firebase functions:shell",
    "start": "npm run shell",
    "backfill:admin-user-type": "node scripts/backfill-admin-user-type.js",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "name": "functions",
  "engines": {
    "node": "22"
  },
  "dependencies": {
    "@genkit-ai/firebase": "^1.13.0",
    "@genkit-ai/vertexai": "^1.13.0",
    "@google-cloud/vision": "^5.3.5",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.4.0",
    "genkit": "^1.13.0",
    "googleapis": "^140.0.1",
    "nodemailer": "^8.0.5",
    "stripe": "^14.21.0"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.31.0",
    "@typescript-eslint/parser": "^8.31.0",
    "eslint": "^8.57.1",
    "eslint-config-google": "^0.14.0",
    "eslint-plugin-import": "^2.25.4",
    "firebase-functions-test": "^3.1.0",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3"
  },
  "private": true
}
